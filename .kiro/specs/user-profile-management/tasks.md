# 用户信息管理任务清单

## 后端任务

- [x] 扩展 `users` 数据模型，新增头像、性别、生日字段。
- [x] 新增或扩展用户资料应用服务，支持当前用户资料查询和更新。
- [x] 新增用户资料控制器 `GET /api/v1/account/profile`、`PUT /api/v1/account/profile`、`POST /api/v1/account/avatar`。
- [x] 扩展图片上传服务，支持 `avatars/` 上传目录和 2MB 头像大小限制。
- [x] 扩展地址服务，支持编辑、删除、设为默认和地址数量上限。
- [x] 扩展地址控制器，新增 `PUT`、`DELETE`、`PATCH /default` 接口。
- [x] 更新登录返回用户视图，包含头像、昵称、性别和生日。

## 前端任务

- [x] 扩展 `mallApi` 用户资料、头像上传、地址编辑、删除、默认地址接口。
- [x] 扩展 `auth` store，保存用户昵称和头像摘要。
- [x] 新增 `/account` 路由和账户页面。
- [x] 更新 `ShopLayout` 登录用户菜单，增加“我的账户”入口和头像/昵称展示。
- [x] 更新 `CheckoutPage` 默认地址选择逻辑。

## 数据库或迁移任务

- [x] 新增 Flyway 迁移 `V5__user_profile_management.sql`。
- [x] 为既有用户设置 `gender='UNKNOWN'` 默认值。
- [x] 确认迁移不会影响已有地址、订单和商品数据。

## 测试任务

- [x] 添加后端用户资料接口测试。
- [x] 添加后端地址管理核心规则测试。
- [x] 执行 `mvn test` 或聚焦模块测试。
- [x] 执行前端 `npm run build`。
- [ ] 手工验证账户页和结算页关键流程。

## 联调与部署任务

- [ ] 启动后端和前端开发服务。
- [ ] 使用登录用户验证资料保存、头像上传、地址增删改默认。
- [ ] 验证刷新页面后用户头像和昵称仍展示正确。
- [ ] 验证结算页自动选中默认地址。

## 验证清单

- [x] 未登录访问 `/account` 会跳转登录页。
- [x] 登录用户只能操作自己的资料和地址。
- [x] 昵称、性别、生日校验符合设计。
- [x] 非图片或超过 2MB 头像上传失败。
- [x] 第一个地址自动设为默认。
- [x] 任意时刻只有一个默认地址。
- [x] 删除默认地址后仍有地址时自动补默认。
- [x] 超过 20 个地址时禁止新增。
- [x] 结算页优先选中默认地址。

## 实现状态

- 需求状态：已确认 MVP 边界。
- 设计状态：已完成初版设计。
- 开发状态：已完成。
- 验证状态：`mvn test` 通过；`npm run build` 通过，存在 Vite chunk size 警告。
